import React from 'react';
import './Header.css';

const Header = () => {
  return (
    <header className="container">
      {/* заголовок */}
      {/* поиск */}
      {/* переключалка языков */}
      {/* иконка профиля */}
    </header>
  );
};

export default Header;
